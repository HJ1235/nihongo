apiVersion: v1
kind: Service
metadata:
  name: nihongo-backend
  namespace: nihongo
spec:
  selector:
    app: nihongo-backend
  ports:
  - name: http
    port: 80
    targetPort: 8080
  type: ClusterIP
